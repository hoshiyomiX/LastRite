name: Deploy to Cloudflare Workers

permissions:
  contents: write

on:
  push:
    branches:
      - main
      - refactor-modular
  pull_request:
    branches:
      - main
      - refactor-modular
  repository_dispatch:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to deploy (informational only, select above)'
        required: false
        default: 'refactor-modular'

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm install

      - name: Deploy and Capture Log
        id: deploy_step
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          # Run deployment and capture output
          set +e
          npx wrangler deploy > deployment.log 2>&1
          EXIT_CODE=$?
          set -e
          
          # Output to console for visibility
          cat deployment.log
          
          # Save exit code for later check
          echo "EXIT_CODE=$EXIT_CODE" >> $GITHUB_ENV

      - name: Commit Deployment Log
        if: always()
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          
          # Pull latest to avoid conflict
          git pull origin ${{ github.ref_name }} --rebase || true
          
          # Create placeholder if log missing
          if [ ! -f deployment.log ]; then
            echo "Deployment step skipped or failed to start." > deployment.log
          fi
          
          # Force add ignored file
          git add -f deployment.log
          
          # Only commit if there are changes
          if git diff --staged --quiet; then
            echo "No changes to deployment log."
          else
            git commit -m "chore: update deployment logs [skip ci]"
            git push origin HEAD:${{ github.ref_name }}
          fi

      - name: Check Deployment Status
        if: always()
        run: |
          if [ -z "$EXIT_CODE" ]; then
             echo "Deployment step did not run."
             exit 1
          fi

          if [ "$EXIT_CODE" -ne 0 ]; then
            echo "Deployment failed with exit code $EXIT_CODE"
            exit $EXIT_CODE
          fi
